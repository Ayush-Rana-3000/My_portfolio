{
  "name": "portfolio-tests",
  "version": "1.0.0",
  "description": "Tests for Ayush Rana's portfolio website",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.12.0",
    "jsdom": "^24.0.0",
    "vitest": "^1.5.0"
  }
}